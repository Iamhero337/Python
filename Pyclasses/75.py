#75. WAP to extract all string data items from the given list and join them with the `*` operator.

s = eval(input('Give the input: '))

elist = []
for i in s:
    if type(i) == str:
        elist.append = i
print(elist)
